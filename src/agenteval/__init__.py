"""agenteval — a benchmark-agnostic evaluation harness for LLM coding agents.

Measures solve rate, records structured trajectories, classifies failure modes,
and supports controlled ablations over agent configuration (context construction,
tool availability, retry strategy, etc.).
"""

__version__ = "0.1.0"
